
#include <stdio.h>
int Main()
{
    int x,y;
    Printf("enter the no. to be divided");
    scanf("%d",&x);
    for(int i=1;i<=5;i+=1);
    {
        x+i=y;
        printf("\n the sum of given number with %d is %d",i,y);
    }
    return 0;
}
